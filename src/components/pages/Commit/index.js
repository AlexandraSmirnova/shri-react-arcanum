import React, { Component } from 'react';
import './styles.scss';

class Commit extends Component {
    render() {
        return (
            <div>
                commit
            </div>
        );
    }
}

export default Commit;