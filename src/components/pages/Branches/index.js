import React, { Component } from 'react';
import './styles.scss';

class Branches extends Component {
    render() {
        return (
            <div>
                branches
            </div>
        );
    }
}

export default Branches;